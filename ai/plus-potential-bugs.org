#+TITLE: bookmark-plus — potential bugs and proposed patches
#+STARTUP: overview

* About this file

This is a report of behaviors in =bookmark-plus= (upstream:
=dmgerman/bookmark-plus=, mirrored from EmacsWiki) that appear to be
bugs. Each entry describes the observed problem, how to reproduce it,
the root cause, and a proposed patch as a =diff= block. Patches are
against the file layout used by =bookmark-plus= (e.g. =bookmark+-1.el=,
=bookmark+-bmu.el=, =bookmark+-lit.el=).

Environment: Emacs 30, modern =display-buffer= behavior, =consult= and =vertico= as the completion UI, =pdf-tools= installed.

For each entry, either merging the patch or requesting changes is
welcome; the goal is to keep =bookmark-plus-gt= a thin layer, not a
carrying repository for hot-fixes.

** Summary

| # | Function / area                        | File             | Type                            | Status                    |
|---+----------------------------------------+------------------+---------------------------------+---------------------------|
| 1 | =bookmark--jump-via=                     | =bookmark+-1.el=   | logic + related lazy help-echo  | applies                   |
| 2 | =bookmark-bmenu-switch-other-window=     | =bookmark+-bmu.el= | display-buffer action           | applies                   |
| 3 | =bmkp-jump-dired=                        | =bookmark+-1.el=   | new =display-buffer= arm          | applies (needs #2)        |
| 5 | =bookmark-write-file=                    | =bookmark+-1.el=   | timestamp cache update          | does not manifest (see body) |
| 6 | consult-driven jump display              | =bookmark+-1.el=   | subsumed by #1                  | conditional               |

(Bug 4 was investigated and removed — it was a bookmark-x-only
issue introduced by the sibling refactor; =bookmark-plus= uses
vanilla =bookmark-bmenu-edit-annotation= which already acts on
point.)

Bugs 1 and 6 are related — the display half of Bug 6 is fixed
by Bug 1's patch. Bug 6's temp-filter half is already
implemented in =bookmark-plus= (=bookmark-write-file= skips
=(bmkp-temporary-bookmark-p bmk)= records), so nothing to do.

Bugs 2 and 3 are related — Bug 2's overriding-action
installation is the caller that Bug 3's handler mishandled.
Both fixes are needed; either alone still shows the symptom in
some scenarios.

Bug 5 is a real gap between =bookmark-plus='s redefined
=bookmark-write-file= and vanilla behavior, but does not produce
the described symptom in a pure =bookmark-plus= session because
=bookmark-plus= also redefines =bookmark-maybe-load-default-file=
(=bookmark+-1.el=:2892) to use a boolean =bmkp-bookmarks-already-loaded=
instead of consulting =bookmark-bookmarks-timestamp=. The
symptom would only appear if vanilla =bookmark-maybe-load-default-file=
somehow ran while =bookmark-plus= was loaded — an unusual state.
Retained for completeness; see the body for the exact reasoning.


* Bug 1 — =bookmark--jump-via= does not display for custom-handler jumps

** Symptom

Jumping to a bookmark whose record has a custom handler (PDF,
EWW, Gnus, Info, Man, image, org, or any third-party handler)
"works" — the destination buffer is created and its handler runs —
but no window changes. The user stays in the previously selected
buffer, unaware anything happened.

Related: when the caller sets =display-buffer-overriding-action=
(e.g. =bookmark-bmenu-switch-other-window=, previewing from
=*Bookmark List*=), some handler families additionally clobber
the originating window in place, so the "preview" replaces the
caller's buffer instead of opening in another window.

** Reproducer

1. Bookmark a PDF via =pdf-tools= (handler
   =pdf-view-bookmark-jump-handler=).
2. From any buffer, =M-x bookmark-jump= that bookmark.
3. Expected: PDF displayed in a window. Observed: nothing visible
   changes; the PDF buffer is alive but never shown.

For the preview variant, do the above from =*Bookmark List*= via
=C-o= and observe the =*Bookmark List*= window get replaced by
the handler's destination.

** Root cause

=bookmark-plus= redefines vanilla =bookmark--jump-via= (in
=bookmark+-1.el=) so that it does *not* call =DISPLAY-FUNCTION=
on the destination after the handler returns. The intent is to
let the handler itself consult the global
=bmkp-jump-display-function= and decide what to do. Only the
default handler path (via =bmkp-goto-position=) actually does
so. Every custom handler follows the vanilla contract
("=set-buffer= to the destination and leave display to the
caller"), so nothing displays.

Handlers fall into three families that need distinguishing:

- *Case A* — =find-file= / =switch-to-buffer= inside the handler.
  These clobber the originating window's buffer in place,
  ignoring =display-buffer-overriding-action=. Hit by
  =org-bookmark-heading-jump= and any handler wrapping
  =find-file=.
- *Case B* — =pop-to-buffer-same-window= inside the handler,
  which honors the overriding action and displays the
  destination in another window itself. Hit by
  =org-goto-marker-or-bmk=.
- *Case C* — pure =set-buffer= handlers (e.g.
  =pdf-view-bookmark-jump-handler=). Caller is responsible for
  displaying.

An unconditional =funcall= of =DISPLAY-FUNCTION= after the
handler is correct only for Case C; Case A leaves the
originating window wrongly clobbered and then displays
elsewhere, and Case B produces a double display that steals a
second window (frequently =*Bookmark List*= in the preview
scenario) for the destination buffer.

** Two approaches to the fix

There are two ways to close the observed gap. Drew has a stated
design preference for the first; the downstream fork's original
patch implements the second. Both close the symptom; they differ
in how much of =bookmark--jump-via='s current contract is
preserved.

*** Approach A — per-handler fixes (Drew's preferred design)

Drew's position is that =bookmark--jump-via= should *not* be
changed to unconditionally call =DISPLAY-FUNCTION= after the
handler. The current behavior is deliberate: it lets a handler
choose to (1) not display at all, (2) invoke
=bmkp-jump-display-function= itself at the point of its
choosing, or (3) display in a way that ignores the caller's
requested action. A vanilla-style =funcall= at the end of
=bookmark--jump-via= removes that flexibility.

Under this approach, each handler that currently does only
=set-buffer= — and therefore silently fails to display under
=bookmark-plus= — is patched individually. The pattern is small:
at the end of the handler body, invoke
=bmkp-jump-display-function= when it is bound and non-nil.

Pattern (illustrated for =xwidget-webkit-bookmark-jump-handler=):

#+begin_src emacs-lisp
(when (and (boundp 'bmkp-jump-display-function)
           bmkp-jump-display-function)
  (save-current-buffer
    (funcall bmkp-jump-display-function (current-buffer)))
  (raise-frame))
#+end_src

Known handlers that need this treatment (non-exhaustive):

- =pdf-view-bookmark-jump-handler= (=pdf-tools=)
- =xwidget-webkit-bookmark-jump-handler= (vanilla Emacs)
- =org-bookmark-heading-jump= and other =find-file=-wrapping
  handlers (case A in the taxonomy above; here the handler
  should perform its own display rather than clobbering the
  current window)
- Any third-party handler that only =set-buffer='s

Trade-off: this preserves =bookmark-plus='s design intent and
avoids interfering with handlers that legitimately do their own
display. Coverage grows one handler at a time, however, and
=bookmark-plus= cannot rescue handlers whose upstream is
unwilling to patch.

*** Approach B — dispatch in =bookmark--jump-via=

Extend =bookmark--jump-via= to detect at run time which of the
three handler families actually ran (see Root cause) and
dispatch the display accordingly. This covers every handler at
once, including third-party ones =bookmark-plus= cannot patch.
The cost is that =bookmark--jump-via='s contract moves closer to
vanilla, which conflicts with the minority of handlers that
intentionally rely on the current =bookmark-plus= behavior of
not displaying, or of displaying non-standardly.

Single patch against =bookmark-plus= =bookmark+-1.el= (line
numbers as of upstream tip; adjust if drift):

#+begin_src diff
diff --git a/bookmark+-1.el b/bookmark+-1.el
--- a/bookmark+-1.el
+++ b/bookmark+-1.el
@@ -2921,7 +2921,29 @@ DISPLAY-FUNCTION is as in `bookmark-jump'."
   (bmkp-record-visit bookmark 'BATCHP)
   (setq bmkp-jump-display-function  display-function)
   (catch 'bookmark--jump-via
-    (bookmark-handle-bookmark bookmark)
+    (let* ((had-handler   (or (bookmark-prop-get bookmark 'file-handler)
+                              (bookmark-get-handler bookmark)))
+           ;; Snapshot the originating window's buffer before the
+           ;; handler runs so we can detect (and undo) in-place
+           ;; clobbers.
+           (orig-window   (and had-handler  display-function (selected-window)))
+           (orig-buffer   (and orig-window  (window-buffer orig-window))))
+      (bookmark-handle-bookmark bookmark)
+      ;; Custom handlers split into three families:
+      ;; A. `find-file'/`switch-to-buffer' clobber orig-window in place.
+      ;; B. `pop-to-buffer-same-window' already displays elsewhere.
+      ;; C. Pure `set-buffer' handlers leave display to the caller.
+      (when (and had-handler  display-function)
+        (cond
+         ;; Case A: undo the clobber, then display.
+         ((and orig-window
+               (window-live-p orig-window)
+               (buffer-live-p orig-buffer)
+               (not (eq (window-buffer orig-window) orig-buffer)))
+          (set-window-buffer orig-window orig-buffer)
+          (funcall display-function (current-buffer)))
+         ;; Case B: handler already displayed; trust it.
+         ((get-buffer-window (current-buffer) t))
+         ;; Case C: display now.
+         (t (funcall display-function (current-buffer))))))
     (unless (and bmkp-use-w32-browser-p  (fboundp 'w32-browser)  (bookmark-get-filename bookmark))
       (let ((win  (get-buffer-window (current-buffer) 0)))
         (when win (set-window-point win (point))))
#+end_src

*** Approach C — dispatch + warn (recommended)

Approaches A and B pull in opposite directions: A preserves
Drew's design (handlers own display) but leaves users hitting
the bug until every handler is patched upstream; B closes the
symptom for everyone but hides the fact that many handlers are
misbehaving.

Approach C combines them. Use Approach B's three-case dispatch
as a *safety net* so users never see the display bug. But in
the cases where the dispatch has to intervene — Case A (handler
clobbered the window, ignoring the caller's overriding action)
and Case C (handler followed the vanilla contract, leaving
display to the caller) — emit a =display-warning= naming the
handler and telling its maintainer exactly what to change.
Case B stays silent (that handler is a good citizen).

Effect: users get correct behavior today, and =bookmark-plus=
advocates for Drew's design at runtime — each affected handler
gets a specific, actionable message that its maintainer will
see in =*Warnings*=. Once handlers are fixed upstream, the
dispatch quietly falls through to Case B for them and the
warnings stop.

A =defcustom= gates the warnings so power users can silence
them if a downstream handler is known-unfixable:

#+begin_src emacs-lisp
(defcustom bmkp-warn-on-handler-display-fix t
  "If non-nil, warn when `bookmark--jump-via' has to fix handler display.
See the entry \"Bug 1\" in bookmark-plus-gt/ai/plus-potential-bugs.org
for context. Set to nil to suppress the warnings while keeping the
safety net."
  :type 'boolean :group 'bookmark-plus)
#+end_src

Patch (supersedes Approach B):

#+begin_src diff
diff --git a/bookmark+-1.el b/bookmark+-1.el
--- a/bookmark+-1.el
+++ b/bookmark+-1.el
@@ -2921,7 +2921,45 @@ DISPLAY-FUNCTION is as in `bookmark-jump'."
   (bmkp-record-visit bookmark 'BATCHP)
   (setq bmkp-jump-display-function  display-function)
   (catch 'bookmark--jump-via
-    (bookmark-handle-bookmark bookmark)
+    (let* ((had-handler   (or (bookmark-prop-get bookmark 'file-handler)
+                              (bookmark-get-handler bookmark)))
+           ;; Snapshot the originating window's buffer before the
+           ;; handler runs so we can detect (and undo) in-place
+           ;; clobbers.
+           (orig-window   (and had-handler  display-function (selected-window)))
+           (orig-buffer   (and orig-window  (window-buffer orig-window))))
+      (bookmark-handle-bookmark bookmark)
+      ;; Custom handlers split into three families:
+      ;; A. `find-file'/`switch-to-buffer' clobber orig-window in place.
+      ;; B. `pop-to-buffer-same-window' already displays elsewhere.
+      ;; C. Pure `set-buffer' handlers leave display to the caller.
+      ;; A and C are display bugs under bookmark-plus's design; we fix
+      ;; the symptom and warn the handler's maintainer.
+      (when (and had-handler  display-function)
+        (cond
+         ;; Case A: undo the clobber, then display.
+         ((and orig-window
+               (window-live-p orig-window)
+               (buffer-live-p orig-buffer)
+               (not (eq (window-buffer orig-window) orig-buffer)))
+          (when bmkp-warn-on-handler-display-fix
+            (display-warning
+             'bookmark-plus
+             (format "Handler `%s' overwrote the current window, ignoring \
+`display-buffer-overriding-action'. It should either honor that action or \
+invoke `bmkp-jump-display-function' on the destination itself." had-handler)
+             :warning))
+          (set-window-buffer orig-window orig-buffer)
+          (funcall display-function (current-buffer)))
+         ;; Case B: handler already displayed the destination; trust it.
+         ((get-buffer-window (current-buffer) t))
+         ;; Case C: handler only set-buffer'd.  Display now.
+         (t
+          (when bmkp-warn-on-handler-display-fix
+            (display-warning
+             'bookmark-plus
+             (format "Handler `%s' did not display the destination. Under \
+bookmark-plus, handlers are expected to consult `bmkp-jump-display-function' \
+and display the destination themselves." had-handler)
+             :warning))
+          (funcall display-function (current-buffer))))))
     (unless (and bmkp-use-w32-browser-p  (fboundp 'w32-browser)  (bookmark-get-filename bookmark))
       (let ((win  (get-buffer-window (current-buffer) 0)))
         (when win (set-window-point win (point))))
#+end_src

*** Recommendation

Land Approach C. It gives users the safety net of Approach B
while advocating for Drew's design (Approach A) through the
warnings. Handler maintainers get a specific, actionable
message; users who don't care about that can flip
=bmkp-warn-on-handler-display-fix= to =nil= and keep the
safety net.

The smallest change common to all three approaches — replacing
=setq= with =let= for =bmkp-jump-display-function= inside
=bookmark--jump-via= — is orthogonal and worth landing on its
own; it keeps the dynamic binding scoped to the jump.

** Related: eager tooltip evaluation on auto-light

Not the same bug but caught in the same debugging pass.
=bmkp-make/move-overlay-of-style= sets =help-echo= to the eager
result of =bmkp-tooltip-content-function=. The default function
calls =image-dired-get-thumbnail-image= and =exiftool= for any
file matching =image-file-name-regexp=. With =pdf-tools=
installed and =pdf= in =image-file-name-extensions=, every
auto-light on a PDF bookmark spawns an ImageMagick PDF
conversion, which fails under default =policy.xml=
(CVE-2018-16509), leaking "Thumb could not be created ... exited
abnormally with code 1" errors. =help-echo= accepts a function;
use it and compute lazily on hover.

#+begin_src diff
diff --git a/bookmark+-lit.el b/bookmark+-lit.el
--- a/bookmark+-lit.el
+++ b/bookmark+-lit.el
@@ -1566,5 +1566,17 @@
     ;; Emacs bug # prevents making use of a face in a `help-echo' tooltip.
     ;; (when ov (overlay-put ov 'help-echo (bmkp-propertize (funcall bmkp-tooltip-content-function bookmark)
     ;;                                                      'face 'bmkp-tooltip-content-face)))
-    (when ov (overlay-put ov 'help-echo (funcall bmkp-tooltip-content-function bookmark)))
+    ;;
+    ;; The `help-echo' property accepts a function -- Emacs only calls it on
+    ;; demand (when the user actually hovers).  Pass a lambda so we do not pay
+    ;; the cost of `bmkp-tooltip-content-function' on every overlay creation.
+    ;; This matters: the default function `bmkp-bookmark-description' runs
+    ;; `image-dired-get-thumbnail-image' and `exiftool' for files matching
+    ;; `image-file-name-regexp'.  Eager evaluation made every auto-light pay
+    ;; that cost (and errored on PDFs when "pdf" was in `image-file-name-extensions',
+    ;; because ImageMagick refuses PDF conversion under default CVE-2018-16509
+    ;; policy).
+    (when ov
+      (overlay-put ov 'help-echo
+                   (lambda (_window _object _pos)
+                     (funcall bmkp-tooltip-content-function bookmark))))
     ov))
#+end_src

** Origin

Downstream commits in =dmgerman/bookmark-x=:
[[https://github.com/dmgerman/bookmark-x/commit/41de219][=41de219=]],
[[https://github.com/dmgerman/bookmark-x/commit/6b94ee4][=6b94ee4=]].
Regression test:
[[https://github.com/dmgerman/bookmark-x/commit/27c7b5e][=27c7b5e=]].
Related =help-echo= laziness fix on =bookmark+-lit.el= is in the
same series.

* Bug 2 — =bookmark-bmenu-switch-other-window= silently full-jumps

** Symptom

=C-o= in =*Bookmark List*=, documented as previewing the
bookmark "while the current window remains selected", instead
replaces the =*Bookmark List*= window's buffer with the
destination — a full jump.

** Reproducer

1. =M-x bookmark-bmenu-list=.
2. Move point to any bookmark.
3. =C-o= (=bookmark-bmenu-switch-other-window=).
4. Expected: destination shown in another window, =*Bookmark
   List*= still selected. Observed: =*Bookmark List*= replaced
   by destination in place.

** Root cause

=bookmark-plus= redefines =bookmark-bmenu-switch-other-window=
in =bookmark+-bmu.el= and let-binds =pop-up-windows=,
=same-window-buffer-names=, and =same-window-regexps= (obsolete
under modern =display-buffer=, Emacs 24+) before calling plain
=display-buffer= with no action. With the default action,
=display-buffer= is free to reuse the selected window for the
destination.

** Proposed patch

Bind =display-buffer-overriding-action= with =(inhibit-same-
window . t)= and wrap in =save-selected-window= so focus returns
to =*Bookmark List*= even if the handler transiently selects the
destination.

#+begin_src diff
diff --git a/bookmark+-bmu.el b/bookmark+-bmu.el
--- a/bookmark+-bmu.el
+++ b/bookmark+-bmu.el
@@ -2172,9 +2172,13 @@ See `bookmark-jump' for info about the prefix arg."
   (interactive "P")
   (bmkp-bmenu-barf-if-not-in-menu-list)
   (bookmark-bmenu-ensure-position)
-  (let ((bookmark-name             (bookmark-bmenu-bookmark))
-        (pop-up-windows            t)
-        (same-window-buffer-names  ())
-        (same-window-regexps       ()))
-    ;; (bookmark-automatically-show-annotations t)) ; $$$$$$ Needed?
-    (bmkp-jump-1 bookmark-name 'display-buffer flip-use-region-p)))
+  (let ((bookmark-name  (bookmark-bmenu-bookmark))
+        ;; Force display in a non-selected window: never reuse the `*Bookmark List*'
+        ;; window for the destination buffer.
+        (display-buffer-overriding-action
+         '((display-buffer-reuse-window
+            display-buffer-use-some-window
+            display-buffer-pop-up-window)
+           (inhibit-same-window . t))))
+    (save-selected-window
+      (bmkp-jump-1 bookmark-name #'display-buffer flip-use-region-p))))
#+end_src

** Note

A separate, optional usability tweak — binding =TAB= (and
=<tab>=) as an alias for =C-o= in =bookmark-bmenu-mode-map=,
matching the convention in =vertico=, =dired-preview=, and
similar list buffers — is not part of this bug fix but can be
added alongside the existing keymap block in =bookmark+-bmu.el=:

#+begin_src emacs-lisp
(define-key bookmark-bmenu-mode-map (kbd "TAB")    'bookmark-bmenu-switch-other-window)
(define-key bookmark-bmenu-mode-map (kbd "<tab>")  'bookmark-bmenu-switch-other-window)
#+end_src

** Origin

Downstream commit in =dmgerman/bookmark-x=:
[[https://github.com/dmgerman/bookmark-x/commit/be10f69][=be10f69=]].

* Bug 3 — =bmkp-jump-dired= ignores =display-buffer=

** Symptom

Previewing a Dired bookmark from =*Bookmark List*= (=C-o=)
replaces the =*Bookmark List*= window's buffer with the Dired
buffer, instead of showing Dired in another window. Only affects
records whose handler is =bmkp-jump-dired= (produced by =bmkp-set=
from inside a dired buffer). Plain file / autofile bookmarks
pointing at the same directory work correctly.

** Reproducer

1. =M-x dired /tmp/=.
2. =bmkp-set= to bookmark the dired buffer (handler
   =bmkp-jump-dired= is stored on the record).
3. =M-x bookmark-bmenu-list=, move to that bookmark, press =C-o=.
4. Expected: Dired opens in another window, =*Bookmark List*=
   remains selected. Observed: =*Bookmark List*= replaced by
   Dired in place.

Note: this reproducer assumes Bug 2's fix is applied. Without it,
the =C-o= caller doesn't install =display-buffer-overriding-action=
in the first place, so the symptom looks the same but originates
from Bug 2, not Bug 3.

** Root cause

=bookmark-bmenu-switch-other-window= (with Bug 2 fixed) installs
=display-buffer-overriding-action= with =(inhibit-same-window .
t)= and dispatches via =display-buffer=. Inside =bmkp-jump-dired=,
the =cl-case= on =bmkp-jump-display-function= folds =display-
buffer= into the same arm as the same-window defaults and calls
plain =dired=, which uses =switch-to-buffer= internally.
=switch-to-buffer= does not consult =display-buffer-overriding-
action=, so it replaces the =*Bookmark List*= buffer in its
window.

There is a second bug hiding behind the first: the tail logic
after the =cl-case= calls =goto-char POS=. If =set-buffer= is not
in effect, point moves in whichever buffer is current — which
may be =*Bookmark List*= — and the *next* read of the bookmark
at point returns the wrong record.

** Proposed patch

Give =display-buffer= its own arm: build via =dired-noselect=,
display through =display-buffer= (which honors the overriding
action), and =set-buffer= so the tail logic runs in the dired
buffer.

#+begin_src diff
diff --git a/bookmark+-1.el b/bookmark+-1.el
--- a/bookmark+-1.el
+++ b/bookmark+-1.el
@@ -11184,8 +11184,19 @@ BOOKMARK is a bookmark name or a bookmark record."
         (subdirs      (bookmark-prop-get bookmark 'dired-subdirs))
         (hidden-dirs  (bookmark-prop-get bookmark 'dired-hidden-dirs)))
     (cl-case bmkp-jump-display-function
-      ((nil bmkp--pop-to-buffer-same-window display-buffer)
+      ((nil bmkp--pop-to-buffer-same-window)
        (dired dir switches))
+      ((display-buffer)
+       ;; `dired' uses `switch-to-buffer' internally, which does not
+       ;; honor `display-buffer-overriding-action'.  Use
+       ;; `dired-noselect' + `display-buffer' so the caller's display
+       ;; action (e.g. `bookmark-bmenu-switch-other-window' previewing
+       ;; in another window) is respected.  Also `set-buffer' to the
+       ;; dired buffer so the tail logic below (subdir insertion,
+       ;; mark restoration, goto-char pos) runs in the right buffer.
+       (let ((buf  (dired-noselect dir switches)))
+         (display-buffer buf)
+         (set-buffer buf)))
       ((bmkp-select-buffer-other-window pop-to-buffer switch-to-buffer-other-window)
        (dired-other-window dir switches))
       (t (dired dir switches)))
#+end_src

** Note for the maintainer

The same shape of bug likely exists in =bmkp-jump-info=,
=bmkp-jump-eww=, =bmkp-jump-man=, and =bmkp-jump-w3m=. Not
audited; each calls a mode entry point that internally uses
=switch-to-buffer= or a display convention that ignores
=display-buffer-overriding-action=. Bug 1's Approach B (three
custom-handler cases in =bookmark--jump-via=) would paper over
the symptoms for those handlers, but a per-handler fix on the
model of this patch is cleaner.

** Origin

Downstream commit in =dmgerman/bookmark-x=:
[[https://github.com/dmgerman/bookmark-x/commit/a07d516][=a07d516=]].

* Bug 5 — Save prompts to reload the file Emacs just wrote

** Symptom

If the user's bookmark file (=bookmark-default-file=, or whichever
file is tracked by =bookmark-bookmarks-timestamp=) is the one being
saved, Emacs prompts on the *next* access to reload it — because
=bookmark-maybe-load-default-file= sees the file's modification
time is newer than the recorded timestamp.

** Reproducer

1. Set =bookmark-default-file= to a shared bookmark file that
   bookmark-plus is also writing (a common setup for users who use
   the built-in =bookmark= functions and =bookmark-plus= in the
   same session).
2. Add a bookmark (=bmkp-set=), let =bookmark-save-flag= write.
3. Perform any command that calls =bookmark-maybe-load-default-file=
   (e.g. =M-x bookmark-jump=).
4. Expected: no prompt. Observed: "Bookmark file X has changed on
   disk. Reload it? (y or n)" for the file Emacs itself just wrote.

** Root cause

=bookmark-plus= redefines =bookmark-write-file= (in
=bookmark+-1.el= at ~line 3553). The redefined function writes
the file but does not update =bookmark-bookmarks-timestamp= —
the mtime cache the built-in loader consults. Vanilla's
=bookmark-write-file= updates the timestamp via
=bookmark-set-bookmarks-timestamp=; the =bookmark-plus=
redefinition dropped that call.

** Proposed patch

Update =bookmark-bookmarks-timestamp= at the end of the write
path when the on-disk file is the one the built-in loader
tracks. Skip the ADD path — that flow writes records that are
not in =bookmark-alist=, and updating the timestamp there would
hide genuine external changes.

#+begin_src diff
diff --git a/bookmark+-1.el b/bookmark+-1.el
--- a/bookmark+-1.el
+++ b/bookmark+-1.el
@@ -3673,4 +3673,19 @@
           (setq bookmark-file-coding-system  coding-system-for-write))
         (unless existing-buf (kill-buffer (current-buffer)))
         (run-hook-with-args 'bmkp-write-bookmark-file-hook file)
+        ;; Keep built-in `bookmark-bookmarks-timestamp' in sync so the
+        ;; built-in `bookmark-maybe-load-default-file' does not prompt
+        ;; to reload the file Emacs itself just wrote.  Only when the
+        ;; on-disk file is the one the built-in loader is tracking, and
+        ;; only for a full write (ADD mode leaves the file with records
+        ;; that are not in `bookmark-alist').
+        (when (and (not errorp)
+                   (not add)
+                   (stringp (car-safe bookmark-bookmarks-timestamp))
+                   (string= (expand-file-name file)
+                            (car bookmark-bookmarks-timestamp)))
+          (setq bookmark-bookmarks-timestamp
+                (cons (car bookmark-bookmarks-timestamp)
+                      (file-attribute-modification-time
+                       (file-attributes file)))))
         (unless errorp (message (concat msg "done") file))))))
#+end_src

** Note

=errorp=, =add=, and =file= are all bound in the enclosing =let=
of =bookmark-write-file=, so no additional arguments or state
are required. Verify against your current tip that
=bookmark-bookmarks-timestamp='s shape is =(FILE . MTIME)= (it
has been since Emacs 27); the =car-safe= / =stringp= guard makes
the code tolerant of an unbound or reshaped value.

** Origin

Downstream commit in =dmgerman/bookmark-x=:
[[https://github.com/dmgerman/bookmark-x/commit/137f219][=137f219=]].

* Bug 6 — Consult-driven jump loads the file but does not display it

This entry is conditional: it presupposes a preview-aware reader
that routes =bookmark-jump= (or an equivalent =bmkp-jump=
variant) through =consult--read=. =bookmark-plus= as shipped
does not do that; the entry is included in case such an
integration lands.

** Symptom

With =consult= and =vertico= as the completion UI, invoking a
jump-through-consult path exhibits this sequence:

1. Preview shows the destination correctly during candidate
   navigation.
2. User selects a candidate (=RET=).
3. Consult clears the preview and returns; the destination file
   is loaded (buffer alive), but no window changes — the user is
   left in the calling buffer, not the destination.

** Root cause (working theory)

Consult's preview callback leaves the destination buffer alive
but undisplayed once the user selects. The subsequent
=bookmark-jump= path relies on =bookmark--jump-via= to call the
display function; for default-handler bookmarks that works, but
for custom-handler bookmarks whose handler only =set-buffer='s
(Bug 1 above), no display happens.

** Proposed change

*Display half.* Fixed by Bug 1 — once =bookmark--jump-via=
either dispatches display itself (Approach B) or every custom
handler learns to invoke =bmkp-jump-display-function= (Approach
A), the consult flow gets its display too. No additional patch
needed here.

*Temporary-bookmark filter.* Already implemented in
=bookmark-plus=: =bookmark-write-file= (=bookmark+-1.el= at
~line 3604) wraps its record dump in
=(unless (bmkp-temporary-bookmark-p bmk) ...)=. Temporary
bookmarks do not survive across sessions. Nothing to add.

If a preview-aware consult wrapper is introduced later and the
display symptom persists after Bug 1's fix, revisit this entry
with a concrete reproducer.

** Origin

Downstream commit in =dmgerman/bookmark-x=:
[[https://github.com/dmgerman/bookmark-x/commit/9fb3420][=9fb3420=]].
The downstream fix is 150+ lines interleaved with the =bmkx-*=
rename. Not adapted here because the two behaviors it targets
(display, temp-filter) are already covered by Bug 1 and by
existing bookmark-plus code.

